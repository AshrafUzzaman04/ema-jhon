import React from "react";

const Products = (props) => {
  //   console.log(props.allproduct.name);
  return (
    <div>
      <h2>{props.allproduct.name}</h2>
    </div>
  );
};

export default Products;
